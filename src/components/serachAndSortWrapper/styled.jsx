import styled from "styled-components";

export const SerachAndSortContainer = styled.section`
    width: 100%;
    margin-top: 47px;
    display: grid;
    grid-template-columns: 1fr min-content;
    gap: 20px;

    .sortMenuWrapper {
        position: relative;
    }
`
