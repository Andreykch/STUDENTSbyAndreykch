import styled from "styled-components";

export const TextLogoContainer = styled.p`
    color: var(--black);

    font-family: "Geometria", Roboto, Arial, serif;
    font-style: normal;
    font-weight: 500;
    font-size: 20px;
    line-height: 44px;

    span{
        color: var(--blue);
    }
`
